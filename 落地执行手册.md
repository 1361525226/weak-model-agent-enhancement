# 落地执行手册 — Agnes 弱模型增强方案

> **目标**：让开发者在 30 分钟内完成从 0 到可用的全栈开发增强环境搭建。
> **前置条件**：已安装 Agnes Code，已登录免费账号。

---

## 第一阶段：基础环境（15 分钟）

### Step 1：安装项目文件

```bash
# 将 agens-agent-patterns 复制到 ZCode Skills 目录
cp -r E:/项目/Agnes提升计划/agens-agent-patterns/skills ~/.zcode/skills/

# 复制 AGENTS.md 到项目根目录
cp E:/项目/Agnes提升计划/AGENTS.md ./AGENTS.md

# 复制工具脚本
cp E:/项目/Agnes提升计划/context-kernel.py ./
cp E:/项目/Agnes提升计划/reflexion_accumulator.py ./
```

### Step 2：创建项目记忆文件

```bash
# 在项目根目录创建
touch CONTEXT.md SUCCESS_PATTERNS.md ERRORS.md
```

**CONTEXT.md 模板：**
```markdown
# 项目共享语言

## 技术栈
- 前端：React + TypeScript + Tailwind
- 后端：Go + PostgreSQL
- 部署：Docker

## 领域术语
- materialization：将配置物化到文件系统
- cascade：级联更新（改一个触发多个）

## 命名约定
- 组件：PascalCase
- 函数：camelCase
- 常量：UPPER_SNAKE_CASE
```

### Step 3：验证 Skill 加载

在 Agnes Code 中执行：
```
/skills list
```
确认看到以下 Skill：
- `loop-engineering`
- `context-engineering`
- `harness-guardrails`
- `memory-system`
- `code-review`
- `debugging`
- `fullstack-dev`
- `multi-agent`

---

## 第二阶段：第一个原子任务（10 分钟）

### 任务示例：实现一个简单的 REST API 端点

**Step 1：开启新会话**
```
/new
```

**Step 2：一次说清意图**
```
目标：在 @src/api/handlers/user.go 中添加 GET /api/users/{id} 端点
验收条件：go test ./... 通过，且返回 JSON {"id":1,"name":"test"}
使用 Thinking 模式（budget=1024）
```

**Step 3：Agnes 执行（遵循 AGENTS.md）**
1. Agnes 读取 AGENTS.md
2. Agnes 加载 @src/api/handlers/user.go（完整路径）
3. Agnes 生成代码
4. Agnes 运行 `go test ./...` 验证
5. 若失败，错误回灌重试（≤3次）
6. 成功则记录到 SUCCESS_PATTERNS.md

**Step 4：复盘**
```
/python reflexion_accumulator.py --task "实现 GET /api/users/{id}" --success
```

---

## 第三阶段：Loop 调试任务（15 分钟）

### 场景：修复一个已知 Bug

**Step 1：收集错误信息**
```bash
# 运行测试，捕获错误
go test ./... 2>&1 | tee /tmp/test_error.log
```

**Step 2：启动调试会话**
```
/new
```

**Step 3：注入错误上下文**
```
目标：修复 @src/api/handlers/user.go 中的 GetUser 函数
错误信息：
$(cat /tmp/test_error.log | head -c 2000)
请使用 debugging Skill 进行 Reflexion 调试，最多重试 3 次。
```

**Step 4：监控 RPM**
```bash
python rpm_monitor.py --watch
```

**Step 5：记录结果**
```bash
# 成功
python reflexion_accumulator.py --task "修复 GetUser null pointer" --success

# 失败
python reflexion_accumulator.py \
  --task "修复 GetUser null pointer" \
  --error "TypeError: Cannot read properties of undefined" \
  --fix-attempt "added null check"
```

---

## 第四阶段：上下文投影（可选，复杂任务前）

### 使用 context-kernel.py

```bash
# 分析当前任务需要哪些文件
python context-kernel.py "实现用户认证功能" --project-root . --budget 4000 --json

# 输出示例：
# {
#   "summary": "实现用户认证功能",
#   "priority_files": ["src/auth/middleware.go", "src/api/handlers/auth.go", ...],
#   "token_estimate": 2800,
#   "skills_to_load": ["fullstack-dev", "harness-guardrails"]
# }
```

然后在新会话中：
```
/new
加载 Skill: fullstack-dev, harness-guardrails
上下文文件：@src/auth/middleware.go, @src/api/handlers/auth.go
目标：实现 JWT 认证中间件
验收：go vet ./... 通过，单元测试覆盖 80%
```

---

## 第五阶段：Code Review（提交前）

### 使用 code-review Skill

```
目标：审查 @src/api/handlers/user.go 的改动
使用 code-review Skill
重点检查：null 安全、SQL 注入、测试覆盖
输出格式：审查报告表格
```

---

## 日常维护

### 每日开始
```bash
# 检查昨日失败模式
cat .loop/ERRORS.md | tail -20

# 查看 RPM 日报
python rpm_monitor.py --report
```

### 每周复盘
```bash
# 生成失败模式汇总
python reflexion_accumulator.py --task "weekly review" --output-dir .loop

# 检查是否需要更新 Skill
cat .loop/FAILURES_REPORT.md
```

### 失败模式积累流程
```
失败发生
    ↓
记录到 ERRORS.md（reflexion_accumulator.py）
    ↓
归类到 error_category
    ↓
若高频出现 → 抽象为 Skill 规则
    ↓
Skill 迭代更新 → 下次自动规避
```

---

## 故障排查

| 问题 | 原因 | 解决 |
|-----|------|------|
| Skill 未触发 | Skill 路径不对 | 确认 `~/.zcode/skills/` 下有对应目录 |
| RPM 频繁超限 | 并行调用过多 | 改为串行，使用 `/new` 隔离 |
| 上下文丢失关键信息 | 截断过早 | 使用完整路径引用，减少无关文件 |
| Loop 空转 | 错误信息不明确 | 确保验证器输出具体错误（非模糊描述） |
| Thinking 模式未开启 | 参数配置错误 | 检查 `chat_template_kwargs.enable_thinking` |

---

## 进阶：Skill 自进化

每遇到重复失败模式，执行以下循环：

```
1. 记录失败（reflexion_accumulator.py）
2. 分析根因（ERRORS.md 中查找）
3. 抽象规则（写成 SKILL.md 条目）
4. 验证效果（下次同类任务不再失败）
5. 提交更新（git commit）
```

**示例：发现 Null Safety 问题高频出现**
```markdown
# 添加到 skills/debugging/SKILL.md

## Null Safety 检查清单
在生成任何访问对象的代码时，必须：
1. 检查对象是否可能为 null/undefined
2. 添加可选链 ?. 或 null check
3. 提供默认值
```

---

## 进度追踪

| 阶段 | 完成标准 | 预计时间 |
|-----|---------|---------|
| 基础环境 | AGENTS.md 生效，Skill 加载正常 | 15 分钟 |
| 第一个任务 | 完成一个 S 级任务并有成功记录 | 10 分钟 |
| Loop 调试 | 完成一个 B 级调试任务 | 15 分钟 |
| 上下文投影 | 使用 context-kernel.py 完成复杂任务 | 10 分钟 |
| Code Review | 完成一次完整审查流程 | 10 分钟 |
| Skill 进化 | 至少更新 1 条失败模式到 Skill | 持续 |
