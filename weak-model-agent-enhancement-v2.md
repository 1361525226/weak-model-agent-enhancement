# 弱模型 Agent 性能增强方案 v2.0

> 核心目标：不增强模型，而是增强 Harness（驱动层），使弱模型在 Agent 驱动下表现更优。

## 背景

同模型在不同 Agent 平台上的性能差异可达 15-22pp。提升的关键在于：

| 因素 | 影响权重 |
|------|---------|
| Harness 质量 | 40% |
| Context 质量 | 30% |
| Loop 效率 | 25% |
| 验证可靠性 | 50% |

## 七大支柱

### 1. 7阶段工作流
需求 → 设计 → 实现 → 验证 → 审查 → 复盘 → 发布
每阶段设置人工单向门，防止误差累积。

### 2. Best-of-N 采样
- 简单任务：串行 R=2
- 中等任务：Best-of-3
- 复杂任务：ToT K=3（树搜索）
- 极复杂：Forest K=5（并行探索）

### 3. Token 预算动态分配（五级）

| 级别 | 场景 | Thinking预算 | 最大Token | 最大轮次 |
|------|------|------------|----------|---------|
| S | 读写小函数 | 关闭 | 1,000 | 1 |
| A | 单模块功能 | 512 | 3,000 | 3 |
| B | 多模块联动 | 1024 | 8,000 | 5 |
| C | 架构设计 | 2048 | 20,000 | 8 |
| X | 高风险操作 | 4096 | 5,000 | 1（人工确认） |

### 4. 语义早停
检测"语义收敛"而非固定轮次，当连续 2 轮 diff 无实质变化时提前停止。
解决 LoopCoder-v2 发现的 R≥3 后性能骤降问题。

### 5. 三层验证
Layer 1: Lint（静态检查）
Layer 2: Test（单元测试）
Layer 3: Build（构建验证）

### 6. 错误回灌
失败时注入精确错误信息，重试 ≤3 次：
```
上次执行失败：
文件：{file_path}:{line_number}
错误：{error_message}
类型：{error_type}
请修复后重新生成。
```

### 7. Skill 自动进化
同类失败 ≥3 次 → 触发 /evolve 命令，将高频失败模式抽象为 Skill 规则，
写入 `~/.zcode/skills/<category>/SKILL.md`。

## 安全护栏

```
SAFE：read, write, execute_script, create_file, update_file
RISKY：rm, delete, deploy, restart_service → 人工确认
BLOCKED：format_disk, wipe_data → 直接拒绝
```

## 性能目标

| 指标 | 基线（v1.0） | 目标（v2.0） |
|------|------------|------------|
| 任务成功率 | ~60% | ≥80% |
| 平均轮次 | 5.2 | ≤3.0 |
| Token 消耗/任务 | 基准 | ≤1.3× |
| 失败回灌成功率 | — | ≥70% |
